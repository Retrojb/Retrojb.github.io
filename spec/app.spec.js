function carousel() {}
describe('carousel', function(){
    it('should return 0 for the main index', function () {
        expect(carousel()).toEqual(0);
    })
})
// made an honest attempt at testing, but have no idea what I am testing for 